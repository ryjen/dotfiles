# Example plaintext shape for secrets.yaml. Encrypt before committing:
#   cp secrets.yaml.example secrets.yaml
#   sops secrets.yaml
# Keys must match modules/home/secrets.nix (sops.secrets.*).
#
# Do not put GPG private keys or ~/.gnupg contents here. Use an explicitly
# encrypted exported subkey artifact instead; see docs/secrets.md.
github_token: ""
openai_api_key: ""
anthropic_api_key: ""
