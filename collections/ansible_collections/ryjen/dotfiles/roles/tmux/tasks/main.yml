---

- name: Install tmux
  include_role:
    name: installer
  vars:
    name: tmux

- name: Install tmux plugin manager
  include_role:
    name: installer
  vars:
    package:
      arch: tmux-plugin-manager
