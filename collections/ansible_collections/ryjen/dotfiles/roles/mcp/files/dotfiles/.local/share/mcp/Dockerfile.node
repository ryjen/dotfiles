FROM node:20-slim

WORKDIR /app

# Install git so npx can fetch GitHub repos
RUN apt-get update && apt-get install -y git && \
    rm -rf /var/lib/apt/lists/*

ENV NODE_ENV=production

EXPOSE 7000

CMD ["npx"]
