FROM python:3.12-slim

# Install git so uvx can fetch GitHub repos
RUN apt-get update && apt-get install -y git && \
    rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir uv

WORKDIR /app

EXPOSE 7000

CMD ["uvx"]
