---
- name: "Ensure oh-my-zsh is {{ install_state }}"
  include_role:
    name: installer
  vars:
    system:
      package: git
    dotfiles: oh-my-zsh

- include_tasks: manual.yml
  when: install_state == 'present'
